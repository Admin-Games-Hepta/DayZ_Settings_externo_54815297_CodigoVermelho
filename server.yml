# server.yml - Configurações de Identidade e Comportamento
# Este arquivo agora é focado em METADADOS do servidor.

ServerID: "codigo_vermelho_ext" # Deve bater com o ID no clients-registry.yml
DisplayName: "CodigoVermelho - Jonathan"

# O Framework usará estes nomes para calcular os caminhos dinamicamente
ServerSettings:
  mission_name: "dayzOffline.chernarusplus"
  
  # Pastas dentro de 'profiles' que o framework deve limpar no WIPE
  wipe_custom_folders:
    - "ExpansionMod/ATM"
    - "ExpansionMod/Quests/PlayerData"

Metadata:
  game: "DayZ"
  client_type: "externo"
  last_updated: "2026-01-22"